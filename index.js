function max(arr){
    
    let m=arr[0];//2 -> 3 -> 4 -> 5
    for(let i=1; i<arr.length; i++){

        if(arr[i]>m){
        //in first loop we compared 2 with arr[1]=1; 2
        //2nd loop, comapring 3>2
        //3rd loop, comapring 4>3
        m=arr[i];
    }
    return pos;
    
}
let marks=[22,91,53,46,52,82]
max(arr);
